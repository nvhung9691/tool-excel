## .NET
bin/
obj/
*.user
.vs/
*.suo

## Rider
.idea/

## Frontend
frontend/node_modules/
# backend/wwwroot là output build của Vite (frontend/) — sinh lại bằng `npm run build`, không commit
backend/wwwroot/

## Secrets — mật khẩu DB thật để ở đây, KHÔNG commit
appsettings.Local.json
appsettings.*.Local.json

## Log của auto-push
auto_push.log

## Log của CI runner trên máy test — ci_test_runner.ps1 ghi vào chính thư mục repo,
## không bỏ qua thì `git status` lấm lem và `git pull --ff-only` vướng
ci_test.log
